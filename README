2017-04-30: I'm trying to convert my resume to HTML. I've tried out a few different options, including using Bootstrap 3, Bootstrap 4 with flex, CSS flexbox, and CSS grid. I have made the most progress with flexbox (in folder: `flex`).
